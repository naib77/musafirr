# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages

# IDE
.idea/
.vscode/
*.iml
.agents

# macOS / iOS
DerivedData/
ios/Flutter/Maps.local.xcconfig

# Web environment (API keys)
web/env.js

# Android
local.properties
**/gradle-wrapper.jar

# Local tooling
.claude/

# Stray Windows device-name artifact
/nul

# Build outputs (build/web is tracked for the web stage deployment)
/build/*
!/build/web

# Local wrangler dev state (workerd cache, asset manifest). Created by
# `wrangler dev` — e.g. tool/verify_link_previews.sh. Never deployed.
.wrangler/
